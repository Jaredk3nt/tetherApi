# Running the project

```
#install packages
npm install

#run mongoDB
mongod

#if installed with homebrew you might need to specify path
mongod --dbpath /usr/local/var/mongodb

#run the server
node server.js
```

